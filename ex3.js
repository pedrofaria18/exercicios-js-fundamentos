/* Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.  */

function exponenciacao(base, expoente) {
    console.log('Exponenciação: ' + (Math.pow(base, expoente)))
}
exponenciacao(2, 3)